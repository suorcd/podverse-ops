# Version: 3 (Added DB Secret Ref)
# FIX: Corrected indentation for envFrom.secretRef.name entries.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-parser-normal
  namespace: podverse-alpha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker-parser-normal
  template:
    metadata:
      labels:
        app: worker-parser-normal
    spec:
      containers:
      - name: worker
        image: ghcr.io/podverse/podverse-workers/podverse-workers:5.1.10-alpha.0
        command: ["npm", "run", "mq_rss_run_parser", "--", "-q", "rss-normal"]
        envFrom:
        - configMapRef:
            name: podverse-workers-config
        - secretRef:
            name: podverse-alpha-db-opaque
        - secretRef:
            name: podverse-alpha-mq-opaque
        # - secretRef:
        #     name: podverse-alpha-workers-opaque            
      #   volumeMounts:
      #   - name: firebase-config
      #     mountPath: /opt/config/firebase-key.json
      #     subPath: firebase-key.json
      #     readOnly: true
      # volumes:
      # - name: firebase-config
      #   secret:
      #     secretName: podverse-alpha-workers-firebase-opaque