# Version: 1
# Application for Message Queue
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: podverse-alpha-mq
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io/podverse-alpha-mq
spec:
  project: default
  source:
    repoURL: https://github.com/suorcd/podverse-ops.git
    targetRevision: feature/k3s01
    path: k8s/alpha/mq
  destination:
    server: https://kubernetes.default.svc
    namespace: podverse-alpha
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
